# 🎬 视频文件管理说明

## ✅ 已添加 Git 忽略规则

视频文件（MP4 等）已添加到 `.gitignore`，**不会上传到 GitHub**。

---

## 📋 忽略的文件类型

### 所有视频格式：
- `*.mp4` - MP4 视频
- `*.avi` - AVI 视频
- `*.mov` - MOV 视频
- `*.wmv` - WMV 视频
- `*.flv` - FLV 视频
- `*.mkv` - MKV 视频
- `*.webm` - WebM 视频

### 特别指定的路径：
- `app/src/main/res/raw/*.mp4`
- `app/src/main/res/raw/*.avi`
- `app/src/main/res/raw/*.mov`

---

## 🔍 检查已忽略的文件

### 查看当前 Git 状态

在项目根目录执行：

```bash
git status
```

**应该看不到** `app/src/main/res/raw/` 目录下的视频文件。

---

## ⚠️ 如果视频文件已经被提交

如果视频文件之前已经提交到 Git，需要从 Git 历史中删除（但保留本地文件）：

### 方法 1：删除单个文件

```bash
git rm --cached app/src/main/res/raw/video_fire_skills.mp4
git rm --cached app/src/main/res/raw/video_cut_chicken.mp4
git rm --cached app/src/main/res/raw/video_chop_ribs.mp4
git commit -m "chore: 从 Git 中移除视频文件"
```

### 方法 2：删除 raw 目录下所有 MP4

```bash
git rm --cached app/src/main/res/raw/*.mp4
git commit -m "chore: 从 Git 中移除视频文件"
```

### 方法 3：删除所有已追踪的 MP4 文件

```bash
git rm --cached -r *.mp4
git commit -m "chore: 从 Git 中移除所有视频文件"
```

**注意：** `--cached` 参数表示只从 Git 中删除，**不删除本地文件**。

---

## 📦 为什么要忽略视频文件？

### 原因：

1. **文件太大** 📊
   - 视频文件通常几 MB 到几十 MB
   - GitHub 单个文件限制：100 MB
   - 仓库总大小建议：< 1 GB

2. **版本控制不适合** 🔄
   - Git 不适合存储二进制大文件
   - 每次修改都会增加仓库大小
   - 历史记录会变得很大

3. **下载速度慢** 🐌
   - 克隆仓库时需要下载所有视频
   - 浪费带宽和时间

4. **GitHub 可能限制** ⚠️
   - 超过限制可能无法推送
   - 可能收到警告邮件

---

## 💡 视频文件管理最佳实践

### 推荐方案 1：使用 Git LFS（大文件存储）

如果确实需要版本控制视频：

```bash
# 安装 Git LFS
git lfs install

# 追踪 MP4 文件
git lfs track "*.mp4"

# 提交 .gitattributes
git add .gitattributes
git commit -m "chore: 添加 Git LFS 支持"
```

**优点：**
- ✅ 可以版本控制
- ✅ 不会让仓库变大
- ✅ GitHub 支持

**缺点：**
- ❌ 需要额外配置
- ❌ LFS 有存储限制（GitHub 免费 1GB）

---

### 推荐方案 2：使用云存储（当前方案 ✅）

视频文件**不提交到 Git**，使用其他方式分发：

#### 选项 A：网盘分享
- 百度网盘
- 阿里云盘
- OneDrive
- Google Drive

#### 选项 B：对象存储
- 阿里云 OSS
- 腾讯云 COS
- AWS S3

#### 选项 C：GitHub Releases
- 在 GitHub Releases 中上传视频
- 不计入仓库大小
- 单个文件限制 2GB

#### 选项 D：本地管理
- 团队内部共享
- U盘拷贝
- 局域网共享

---

## 📝 在 README 中说明

建议在 `README.md` 中添加说明：

```markdown
## 📹 视频文件说明

视频文件存放在 `app/src/main/res/raw/` 目录下，但**不包含在 Git 仓库中**。

### 获取视频文件

请通过以下方式获取：
- [百度网盘链接]()
- [阿里云盘链接]()
- 或联系项目维护者

### 视频文件列表

需要以下视频文件：
- `video_fire_skills.mp4` - 生火技巧
- `video_cut_chicken.mp4` - 砍鸡的方法  
- `video_chop_ribs.mp4` - 砍排骨方法

### 放置位置

下载后将视频文件放入：
```
app/src/main/res/raw/
```
```

---

## 🔒 保留 .gitkeep 文件

`app/src/main/res/raw/.gitkeep` 文件会被保留：
- ✅ 确保 `raw` 目录存在于 Git 中
- ✅ 其他开发者克隆后有这个目录
- ✅ 只需要把视频文件放进去即可

---

## ✅ 验证配置

### 1. 检查 .gitignore

```bash
cat .gitignore
```

应该看到：
```
# Video files (too large for GitHub)
*.mp4
*.avi
...
```

### 2. 检查 Git 状态

```bash
git status
```

**不应该**看到 `*.mp4` 文件被追踪。

### 3. 测试忽略规则

```bash
# 查看哪些文件会被忽略
git status --ignored
```

应该看到视频文件在"Ignored files"部分。

---

## 🚀 下次提交时

```bash
# 添加所有更改（自动忽略 .gitignore 中的文件）
git add .

# 查看即将提交的文件（确认没有视频文件）
git status

# 提交
git commit -m "feat: 添加视频播放功能"

# 推送到 GitHub
git push
```

**视频文件会被自动忽略，不会上传到 GitHub！** ✅

---

## 📊 文件大小对比

| 类型 | 大小 | 是否提交 |
|------|------|----------|
| 代码文件 | < 1 MB | ✅ 提交 |
| 图片资源 | < 500 KB | ✅ 提交 |
| **视频文件** | **几 MB - 几十 MB** | ❌ **不提交** |
| 文档 Markdown | < 100 KB | ✅ 提交 |

---

**配置完成日期：** 2026-01-01  
**状态：** ✅ 视频文件已添加到 .gitignore  
**建议：** 使用云存储或 GitHub Releases 分发视频文件

